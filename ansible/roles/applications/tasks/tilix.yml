- name: Add Tilix repo
  become: yes
  become_user: root
  apt_repository:
    codename: '{{ ubuntu_release }}'  
    repo: 'ppa:webupd8team/terminix'
    update_cache: yes

- name: Install Tilix
  become: yes
  become_user: root
  apt:
    name: tilix
